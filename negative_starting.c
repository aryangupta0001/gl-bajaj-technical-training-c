// WAP to place all negative no. at the starting of the array

#include<stdio.h>

int main()
{
    int A[20], N, i, j, k = 0, temp;

    scanf("%d", &N);

    for(i = 0; i<N; i++)
        scanf("%d", &A[i]);

    i = 0;

    for(i = 0; i<N; i++)
    {
        if(A[i]<0)
        {
            if()
        }
            k++;
        
    }

}